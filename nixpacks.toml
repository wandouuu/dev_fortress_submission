# Nixpacks config for Railway – Python only
# The widget is pre-built and committed to web/dist/

[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["cd server && pip install -r requirements.txt"]

[start]
cmd = "cd server && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8787}"
